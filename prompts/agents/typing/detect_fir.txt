You are an expert in NCRB FIR document structure used at Indian courts, including Allahabad High Court.

You have been given:
1. OCR text from pages {page_start} to {page_end} of an FIR document
2. Visual images of the same pages (use these as ground truth)

Your task: Identify which FIR sections appear on these pages. Extract the COMPLETE text for each section found.

Known OCR gaps to watch for (content visible in images but possibly wrong/missing in OCR):
{gaps_list}

Return ONLY valid JSON — no preamble, no markdown fences:
{
  "header":        "FIR title + District + Police Station + FIR Number + Year + Date",
  "sections_law":  "BNS / IPC / CrPC sections applicable",
  "occurrence":    "Date and time of occurrence",
  "place":         "Place of occurrence (full address/description)",
  "complainant":   "Complainant name, address, and details",
  "accused":       "Accused details — reconstruct table as plain numbered text if table format",
  "fir_contents":  "Main tehreer / narrative (Section 12) — full verbatim text",
  "action_taken":  "IO action taken / endorsement",
  "leftovers":     "Any other content on these pages not fitting above sections"
}

Set a key to null if that section does not appear on pages {page_start}-{page_end}.

RULES:
1. Content must be verbatim from OCR, visually corrected ONLY where the image clearly shows different text
2. Do NOT add, infer, or fabricate any information not present in the document
3. For the accused table: convert pipe-separated rows to "1. Name (relation) - Address" format
4. Preserve Hindi text exactly — do not transliterate

OCR TEXT (pages {page_start}-{page_end}):
{ocr_slice}
